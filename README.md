# NityansApp - Complete Production Flutter Campus Portal

## Quick Start
```bash
flutter pub get
flutter run
flutter build apk --release
```

## Platforms Included
- Android (`/android`)
- iOS (`/ios`)
- Web (`/web`)
- Linux (`/linux`)
- macOS (`/macos`)
- Windows (`/windows`)
- Unit & Widget Tests (`/test`)
